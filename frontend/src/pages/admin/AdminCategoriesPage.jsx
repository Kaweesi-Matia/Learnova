import { useEffect, useState } from "react";
import DashboardLayout from "../../layouts/DashboardLayout";
import api from "../../services/api";
export default function AdminCategoriesPage(){const[categories,setCategories]=useState([]),[name,setName]=useState(""),[description,setDescription]=useState(""),[editing,setEditing]=useState(null),[loading,setLoading]=useState(true);const load=async()=>{try{setLoading(true);setCategories((await api.get('/admin/categories')).data.categories||[])}finally{setLoading(false)}};useEffect(()=>{load()},[]);const save=async()=>{if(!name.trim())return;if(editing)await api.put(`/admin/categories/${editing}`,{name,description});else await api.post('/admin/categories',{name,description});setName('');setDescription('');setEditing(null);load()};const edit=c=>{setEditing(c._id);setName(c.name);setDescription(c.description||'')};const del=async id=>{if(!confirm('Delete this category?'))return;try{await api.delete(`/admin/categories/${id}`);load()}catch(e){alert(e.response?.data?.message||'Delete failed')}};return <DashboardLayout role="ADMIN"><div className="space-y-6"><div><p className="eyebrow">Admin portal</p><h1 className="mt-2 text-3xl font-black">Categories</h1><p className="mt-2 text-slate-500">Create and organize the marketplace categories.</p></div><div className="card p-6"><h2 className="font-bold">{editing?'Edit category':'Add category'}</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><input className="input" placeholder="Category name" value={name} onChange={e=>setName(e.target.value)}/><input className="input" placeholder="Description" value={description} onChange={e=>setDescription(e.target.value)}/></div><div className="mt-4 flex gap-2"><button className="btn-primary" onClick={save}>{editing?'Save changes':'Add category'}</button>{editing&&<button className="btn-secondary" onClick={()=>{setEditing(null);setName('');setDescription('')}}>Cancel</button>}</div></div><div className="card overflow-hidden"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Name</th><th className="p-4">Slug</th><th className="p-4">Description</th><th className="p-4">Actions</th></tr></thead><tbody>{loading?<tr><td className="p-5" colSpan="4">Loading...</td></tr>:categories.map(c=><tr className="border-t" key={c._id}><td className="p-4 font-semibold">{c.name}</td><td className="p-4">{c.slug}</td><td className="p-4">{c.description||'—'}</td><td className="p-4 flex gap-3"><button className="text-brand-600" onClick={()=>edit(c)}>Edit</button><button className="text-red-600" onClick={()=>del(c._id)}>Delete</button></td></tr>)}</tbody></table></div></div></DashboardLayout>}
