import { useEffect,useState } from "react";import DashboardLayout from "../../layouts/DashboardLayout";import api from "../../services/api";
export default function AdminCoursesPage(){const[courses,setCourses]=useState([]),[loading,setLoading]=useState(true);const load=async()=>{try{setLoading(true);setCourses((await api.get('/admin/courses')).data.courses||[])}catch(e){alert(e.response?.data?.message||'Unable to load courses')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const toggle=async c=>{try{await api.put(`/admin/courses/${c._id}`,{published:!c.published});load()}catch(e){alert(e.response?.data?.message||'Update failed')}};const del=async id=>{if(!confirm('Delete this course?'))return;try{await api.delete(`/admin/courses/${id}`);load()}catch(e){alert(e.response?.data?.message||'Delete failed')}};return <DashboardLayout role="ADMIN"><div className="space-y-6"><div><p className="eyebrow">Admin portal</p><h1 className="mt-2 text-3xl font-black">Courses</h1><p className="mt-2 text-slate-500">Review, publish and remove courses across LearnHub.</p></div><div className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Course</th><th className="p-4">Instructor</th><th className="p-4">Category</th><th className="p-4">Price</th><th className="p-4">Status</th><th className="p-4">Actions</th></tr></thead><tbody>{loading?<tr><td className="p-5" colSpan="6">Loading...</td></tr>:courses.map(c=><tr className="border-t" key={c._id}><td className="p-4 font-semibold">{c.title}</td><td className="p-4">{c.instructor?.name||'—'}</td><td className="p-4">{c.category?.name||'—'}</td><td className="p-4">${Number(c.price||0).toFixed(2)}</td><td className="p-4">{c.published?'Published':'Draft'}</td><td className="p-4 flex gap-3"><button className="text-brand-600" onClick={()=>toggle(c)}>{c.published?'Unpublish':'Publish'}</button><button className="text-red-600" onClick={()=>del(c._id)}>Delete</button></td></tr>)}</tbody></table></div></div></div></DashboardLayout>}
