import { useEffect,useState } from "react";import {Link} from "react-router-dom";import {BookOpen,FolderTree,MessageSquare,Users,GraduationCap,DollarSign} from "lucide-react";import DashboardLayout from "../../layouts/DashboardLayout";import {useAuth} from "../../context/AuthContext";import api from "../../services/api";
export default function AdminDashboard(){const{user}=useAuth();const[stats,setStats]=useState(null);useEffect(()=>{api.get('/admin/stats').then(r=>setStats(r.data)).catch(console.error)},[]);const cards=[['Users',stats?.users??'—',Users,'/admin/users'],['Students',stats?.students??'—',GraduationCap,'/admin/users'],['Courses',stats?.courses??'—',BookOpen,'/admin/courses'],['Revenue',stats?`$${Number(stats.revenue||0).toFixed(2)}`:'—',DollarSign,'/admin/courses']];return <DashboardLayout role="ADMIN"><div className="space-y-8"><div><p className="eyebrow">Admin dashboard</p><h1 className="mt-2 text-3xl font-black">Welcome back, {user?.name||'Admin'} 👋</h1><p className="mt-2 text-slate-500">Manage LearnHub from one place.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon,to])=><Link to={to} className="card p-5" key={label}><div className="flex justify-between"><div><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></div><Icon className="text-brand-600"/></div></Link>)}</div><div className="grid gap-4 md:grid-cols-4"><Link className="btn-primary" to="/admin/courses">Manage Courses</Link><Link className="btn-secondary" to="/admin/categories">Manage Categories</Link><Link className="btn-secondary" to="/admin/users">Manage Users</Link><Link className="btn-secondary" to="/admin/reviews">Manage Reviews</Link></div><div className="card p-6"><h2 className="text-xl font-bold">Platform overview</h2><div className="mt-5 grid grid-cols-2 gap-4 md:grid-cols-3"><Metric label="Instructors" value={stats?.instructors??'—'}/><Metric label="Enrollments" value={stats?.enrollments??'—'}/><Metric label="Reviews" value={stats?.reviews??'—'}/></div></div></div></DashboardLayout>};function Metric({label,value}){return <div className="rounded-xl bg-slate-50 p-4"><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div>}
