import { useEffect,useState } from "react";import DashboardLayout from "../../layouts/DashboardLayout";import api from "../../services/api";
export default function AdminReviewsPage(){const[reviews,setReviews]=useState([]),[loading,setLoading]=useState(true);const load=async()=>{try{setLoading(true);setReviews((await api.get('/admin/reviews')).data.reviews||[])}catch(e){alert(e.response?.data?.message||'Unable to load reviews')}finally{setLoading(false)}};useEffect(()=>{load()},[]);const del=async id=>{if(!confirm('Delete this review?'))return;try{await api.delete(`/admin/reviews/${id}`);load()}catch(e){alert(e.response?.data?.message||'Delete failed')}};return <DashboardLayout role="ADMIN"><div className="space-y-6"><div><p className="eyebrow">Admin portal</p><h1 className="mt-2 text-3xl font-black">Reviews</h1><p className="mt-2 text-slate-500">Moderate student reviews and keep the marketplace healthy.</p></div><div className="space-y-3">{loading?<div className="card p-6">Loading...</div>:reviews.length===0?<div className="card p-8 text-slate-500">No reviews yet.</div>:reviews.map(r=><div className="card p-5" key={r._id}><div className="flex items-start justify-between gap-4"><div><p className="font-bold">{r.course?.title||'Course'}</p><p className="text-sm text-slate-500">{r.student?.name||'Student'} · {r.student?.email||''}</p><p className="mt-2">{'★'.repeat(r.rating||0)} <span className="text-slate-400">({r.rating}/5)</span></p><p className="mt-3 text-slate-600">{r.comment||'No comment'}</p></div><button className="text-red-600" onClick={()=>del(r._id)}>Delete</button></div></div>)}</div></div></DashboardLayout>}
